const statusRoom = {
  EMPTY: 0, // phong trong
  HIRED: 1, // phong da thue
};
const statusBuilding = {
  EMPTY: 0, // phong trong
  HIRED: 1, // phong da thue
};

const status = { statusRoom, statusBuilding };
module.exports = status;
